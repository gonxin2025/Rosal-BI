# Chatbot (pendiente)

Espacio reservado para una interfaz de chat dedicada, separada del
formulario de pregunta en lenguaje natural que ya existe dentro de
`frontend/dashboard/`.

Idea: mover aquí una experiencia de chat más conversacional (historial
de mensajes, seguimiento de contexto entre preguntas) que consuma el
mismo webhook de n8n documentado en `docs/api.md`.

Sin desarrollar todavía — no forma parte del alcance actual del
prototipo.

# limpieza

Scripts de limpieza/normalizacion antes de mover un dataset de `originales/` a `procesados/` (encoding, fechas, valores vacios).

Sin scripts todavia -- carpeta preparada para cuando se necesiten.
